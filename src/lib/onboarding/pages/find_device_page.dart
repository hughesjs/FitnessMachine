import 'package:flutter/material.dart';

class FindDevicePage extends StatelessWidget {
  const FindDevicePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Find Device'));
  }
}